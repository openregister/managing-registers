= content_for :page_title, "Register"

%header.page-header
  .grid-row
    .column-two-thirds
      %h1.heading-large #{prepare_register_name(@register_name)} Register
    .column-one-third
      = link_to "Add a form #{prepare_register_name(@register_name)}", "#{@register_name}/new", class: "button align-with-heading"

.js-filter-list
  %form{class: "js-filter-form"}
    %fieldset
      .form-group
        %label{class: "form-label", for: "search"} Search for a #{prepare_register_name(@register_name)}
        %input{name: "search", id: "search", class: "form-control"}

%table
  %thead
    %tr
      %th #{@register_name.capitalize}
      - if @register[0].try(:name)
        %th Name
      %th

  %tbody{class: "js-filter-block"}
    - @register.each do |register|
      %tr{class: "js-filter-item", "data-filter-terms" => "#{register.name if register.try(:name)} #{register.key}"}
        %td= register.key
        - if register.try(:name)
          %td= register.name
        %td{style:"text-align:right"}= link_to 'Update', "#{@register_name}/#{register.key}/edit"

= content_for :javascript do
  :javascript
    jQuery(function($) {
      GOVUK.filterListItems.init();
    });
